.row.mb-3
  .col-md-8
    %h2 Twoje rozmowy
  .col-md-4.text-md-end
    = link_to "Nowa rozmowa", new_conversation_path, class: "btn btn-primary"

.row
  .col-md-12
    - if @conversations.any?
      - @conversations.each do |conversation|
        .card.mb-2
          .card-body.d-flex.justify-content-between.align-items-center
            %div
              %h5.card-header
                %small.text-muted Autor:
                = conversation&.author.name || "No Creator"
                = avatar_for(conversation.author)
              %small.text-muted
                Utworzono:
                = l(conversation.created_at, format: :short)
            %h4.card-title
              = link_to conversation.subject, conversation

            %div
              = button_to "Usuń",
                          conversation_path(conversation),
                          method: :delete,
                          data: { confirm: "Na pewno?" },
                          class: "btn btn-danger btn-sm"

    - else
      .alert.alert-info
        Nie masz jeszcze żadnych rozmów. Kliknij „Nowa rozmowa”, żeby zacząć.
